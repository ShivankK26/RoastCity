# RoastCity Environment Variables
# Copy to .env.local (Next.js) or .env (Express) and fill as needed.

# -----------------------------------------------------------------------------
# Optional – no env required for local dev (in-memory store)
# -----------------------------------------------------------------------------

# Server
PORT=3000

# -----------------------------------------------------------------------------
# Upstash Redis (optional – for persistent storage)
# Get credentials: https://console.upstash.com/
# -----------------------------------------------------------------------------
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# -----------------------------------------------------------------------------
# Alchemy RPC (optional – for spectator token verification on Base)
# -----------------------------------------------------------------------------
# ALCHEMY_RPC_URL=

# -----------------------------------------------------------------------------
# PostgreSQL (optional – if you add a SQL-backed store)
# -----------------------------------------------------------------------------
# DATABASE_URL=postgresql://user:password@host:5432/roastcity
